# Spring application configuration
spring.application.name=event-service
server.port=8082

# Kafka broker configuration
spring.cloud.stream.kafka.binder.brokers=localhost:9092
spring.cloud.stream.kafka.binder.zkNodes=localhost:2181

# Kafka topics (for event publishing and consumption)
# Uncomment if events are used
# spring.cloud.stream.bindings.eventUpdated-out-0.destination=event-updated
# spring.cloud.stream.bindings.eventUpdated-out-0.contentType=application/json
